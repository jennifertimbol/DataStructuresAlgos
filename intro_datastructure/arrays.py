new_list = [1, 2, 3]
# Accessing
result = new_list[0]

# Search
if 1 in new_list: print(True)

for n in new_list:
    if n == 1:
        print(True)

        break

# Inserting 2 ways
# Linear run time, if you insert in the front it'll move all values to the right
# Appending just adds it to position at the end

